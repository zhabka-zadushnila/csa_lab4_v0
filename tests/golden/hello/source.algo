fn print_str(ptr s) -> i32 {
    i32 i = 0;
    while (s[i] != 0) {
        cout << s[i];
        i = i + 1;
    }
    return 0;
}

fn main() -> i32 {
    ptr msg = "Hello, World!";
    print_str(msg);
    return 0;
}
