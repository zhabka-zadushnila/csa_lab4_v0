fn main() -> i32 {
    ptr arr[512];
    for (i32 i = 0; i < 512; i = i + 1) {
        arr[i] = i;
    }
    i32 sum = 0;
    i32 i = 0;
    while (i < 512) {
        sum = sum + arr[i];
        i = i + 1;
    }
    cout << sum;
    return 0;
}
