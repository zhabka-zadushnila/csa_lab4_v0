# TODO.md: План разработки симулятора процессора

Этот документ описывает поэтапный план реализации микропрограммного процессора на Rust в соответствии с техническим заданием.

## Этап 1: Определение базовых структур и типов данных
- [x] Описать структуру регистров процессора (`Registers`) и флагов состояния (`ProcessorStatus`).
- [x] Описать структуру декодированной микрокоманды (`MicroInstruction`) и реализовать парсинг из `u64`.
- [x] Определить константы для управления шиной (маски для `bus_out`, `bus_in`, сигналов АЛУ, константы адресов MMIO).

## Этап 2: Подсистема памяти и кэширования
- [x] Реализовать структуру кэша L1 (Direct-mapped, 128 строк по 16 слов).
- [x] Написать логику разделения 32-битного адреса на `tag` (19 бит), `index` (7 бит) и `offset` (6 бит).
- [x] Реализовать метод доступа к памяти через кэш:
  - [x] Логика проверки попадания (hit) — 1 такт.
  - [x] Логика обработки промаха (miss) — 10 тактов задержки, вытеснение грязной (dirty) строки, загрузка новой строки из основной памяти.
- [x] Добавить обработку портов ввода-вывода (MMIO):
  - [x] Перехват адреса `0x0000` (чтение из `in_stream` с возможной остановкой конвейера при отсутствии токенов).
  - [x] Перехват адреса `0x0004` (запись в `out_stream`).

## Этап 3: Функциональный блок АЛУ (ALU)
- [x] Реализовать выполнение базовых целочисленных операций: `ADD`, `SUB`, `MUL`, `DIV`, `MOD`, `AND`, `OR`, `XOR`, `CMP`.
- [x] Реализовать унарные операции: `INV`, `ABS`, `INC`, `DEC`.
- [x] Реализовать вспомогательные операции: знаковое расширение `EXT8`, `EXT16`.
- [x] Написать расчет флагов `Z` (Zero), `N` (Negative), `C` (Carry), `V` (Overflow) по результатам операций.

## Этап 4: Микропрограммный движок (Sequencer)
- [+] Реализовать логику выбора следующего состояния MPC (Micro Program Counter):
  - [+] `NEXT` (MPC + 1)
  - [+] `JUMP` (переход на `next_addr` при выполнении условия `seq_cond`)
  - [+] `MAP` (переход на адрес из Mapping ROM на основе опкода текущей инструкции в CR)
- [+] Написать логику условных переходов на уровне микрокода (проверка флагов PS и режимов адресации).
- [+] Заполнить Mapping ROM для всех инструкций процессора.

## Этап 5: Разработка управляющего микрокода (Firmware)
- [x] Написать базовый цикл выборки инструкции (Fetch Cycle) в памяти микропрограмм.
- [x] Реализовать микропрограммы для резолюции адресов (косвенный, автоинкрементный, автодекрементный, стек).
- [x] Написать микрокод для всех основных инструкций (пересылки, арифметика, переходы, вызовы функций).

## Этап 6: Главный цикл симулятора и интеграция
- [x] Реализовать метод загрузки бинарного образа программы (код, секция данных, BSS) в RAM перед стартом.
- [x] Реализовать потактовый цикл выполнения (`step()`):
  - [x] Выборка микрокоманды по `MPC`.
  - [x] Выполнение микроопераций (коммутация шины, запуск АЛУ, обращение к памяти).
  - [x] Обновление состояния и инкремент счетчика тактов `ticks`.
- [x] Добавить механизм остановки симулятора при достижении инструкции `HLT`.

## Этап 7: Тестирование и верификация
- [ ] Написать тесты для верификации работы кэш-памяти (попадания, промахи, вытеснение).
- [ ] Протестировать сквозное выполнение тестовых программ:
  - [ ] Программа 1 (математическое выражение).
  - [ ] Программа 2 (взятие модуля).
  - [ ] Программа 6 (ввод-вывод с эмуляцией потока).
- [ ] Сравнить временные характеристики (количество тактов) выполнения программ с кэшем и без него.